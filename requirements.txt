# MCP runtime — `mcp[cli]` provides the `mcp run` launcher used in containers.
mcp[cli]>=1.3.0

# Google Ads API — gRPC client (the codebase migrated off REST in 9d27ca9).
google-ads>=29.2.0

# OAuth helper used only by setup_oauth.py to mint refresh tokens.
google-auth-oauthlib>=1.2.1

# HTTP client used by download_image_asset.
requests>=2.31.0

# Optional .env loader (skipped silently if not present).
python-dotenv>=1.0.0
